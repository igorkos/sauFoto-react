module.exports = {
    dependencies: {
        "react-native-threads": {
            platforms: {
                android: null,
                //ios: null
                // add more platform to disable auto-linking for them too
            }
        }
    }
}
